<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="priority">110</field>
        <field name="arch" type="xml">
			<xpath expr="//div[./button[@name='action_activate_currency']][1]" position="replace" mode="inner">
                In order to validate this bill, you must <button class="oe_link" type="object" name="action_activate_currency" style="padding: 0; vertical-align: baseline;">activate the currency of the bill</button>. The journal entries need to be computed by Viindoo before being posted in your company's currency.
            </xpath>
            <xpath expr="//div[./button[@name='action_activate_currency']][2]" position="replace" mode="inner">
				In order to validate this invoice, you must <button class="oe_link" type="object" name="action_activate_currency" style="padding: 0; vertical-align: baseline;">activate the currency of the invoice</button>. The journal entries need to be computed by Viindoo before being posted in your company's currency.
            </xpath>
        </field>
    </record>
</odoo>
